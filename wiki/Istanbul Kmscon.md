#service/kmscon #host/istanbul #status/active #area/access

> [!abstract] Purpose
> Kmscon provides the local console environment for Istanbul; the shared console settings remain available as a fallback.

## Configuration

The module is imported by Istanbul's active module set. Console behavior is complemented by the shared settings in [[Shared Baseline]].

[^source]: `hosts/istanbul/modules/kmscon.nix`, `configuration.nix`
