#service/kmscon #scope/fleet #status/active #area/access

> [!abstract] Purpose
> Kmscon is the common console service imported by the hosts, with shared console fallback settings.

## Baseline

Host Kmscon modules provide the local console experience. Shared console settings and packages are defined in the baseline configuration.

[^source]: `configuration.nix`, `hosts/*/modules/kmscon.nix`
